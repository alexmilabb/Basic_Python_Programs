coordinates = (4, 5)  # --> a tuple is immutable - meaning that you can't change elements here in any way

print(coordinates[0])

# a list uses [] instead of (), lists can be changed, but tuples cannot